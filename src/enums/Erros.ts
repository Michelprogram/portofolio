enum Errors {
  fields = "⚠️ One or many fields are wrong",
  email = "⚠️ Email is wrong.",
  good = "🚀",
  sent = "😃 Message successfully sent",
  errMailJs = "💥 Oh we have some troubles in back side",
}

export default Errors;
